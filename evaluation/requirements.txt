# common
vllm
tqdm
datasets
torch
transformers
python-dateutil
# flash_attn  # Optional, install after torch if needed

# math_eval
sympy==1.12
antlr4-python3-runtime==4.11.1 # ! The version needs to be compatible with sympy.
word2number
Pebble
timeout-decorator